# OpenDeviceIO — local environment template.
# Copy this file to `.env` (which is gitignored) and fill in your values.
#
# Genie's Claude extraction backend reads this key:
ANTHROPIC_API_KEY=

# Optional: override the canonical schema location used by Genie.
# ODIO_SCHEMA_PATH=
